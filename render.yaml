services:
  - type: web
    name: youtube-sync
    runtime: node
    buildCommand: cd server && npm install --production
    startCommand: node server/index.js
    envVars:
      - key: PORT
        value: 10000
