{
  "name": "youtube-sync",
  "version": "1.0.0",
  "description": "Sync YouTube playback across devices",
  "scripts": {
    "build": "cd server && npm install --production",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
